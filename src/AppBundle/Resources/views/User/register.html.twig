{% extends "AppBundle:Layouts:layout.html.twig" %}

{% block content %}
    <div class="box-form col-lg-8">
        <h2>Registrarse</h2>
        <hr>

        {{form_start(form, {'action': '', 'method': 'POST'})}}
        {{form_errors(form)}}
        {{form_end(form)}}
    </div>
{% endblock %}

{% block javascripts %}
  {{ parent() }}
  <script type="text/javascript" src="{{ asset('assets/js/custom/nick-test.js') }}"></script>
{% endblock %}